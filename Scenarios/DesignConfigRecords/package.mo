within NISSA_12UCubeSat.Scenarios;
package DesignConfigRecords "按分系统与设备实例组织的设计配置记录"
  extends Modelica.Icons.RecordsPackage;
  annotation(Documentation(info="<html><p>仅定义配置字段及分层结构，不保存数值默认值。数值默认事实源是DefaultSpacecraftDesignConfig。</p></html>"));
end DesignConfigRecords;
